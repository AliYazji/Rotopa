# ── Legacy source (SQL Server) ────────────────────────────────────────────────
MSSQL_HOST=localhost
MSSQL_PORT=14330
MSSQL_USER=sa
MSSQL_PASSWORD=Rotopa!Pass2024
MSSQL_DATABASE=acc
MSSQL_ENCRYPT=false

# ── Target (Postgres — a direct connection that BYPASSES RLS) ──────────────────
# Local test DB:   postgres://postgres:postgres@localhost:55432/rotopa
# Supabase Cloud:  postgres://postgres:<pw>@db.<ref>.supabase.co:5432/postgres
TARGET_DATABASE_URL=postgres://postgres:postgres@localhost:55432/rotopa

# ── Which organization to load into ──────────────────────────────────────────
ORG_CODE=RETAJ
ORG_NAME_AR=مجموعة رتاج
# Legacy Lockup 'Currancy' FieldNo to treat as the base currency (1 = شيكل)
BASE_CURRENCY_LEGACY_NO=1
FISCAL_YEAR_START_MONTH=1

# Optional: an existing auth.users id to attach as the organization owner.
# Leave blank for a data-only load (e.g. into the test DB).
ADMIN_USER_ID=
